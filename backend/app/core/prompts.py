CONVERT_PROMPT = """你是专业的财务文件结构化引擎。请分析上传的文件，提取完整结构并返回 JSON。

要求返回如下结构：
{
  "documentType": "delivery_note|reconciliation|quotation|purchase_order|bank_statement|payment_list|other",
  "title": "文档标题，如：送 货 单、报 价 单、对 账 单",
  "companyInfo": {
    "name": "公司名称",
    "address": "地址",
    "phone": "电话",
    "fax": "传真",
    "other": ["其他头部信息行"]
  },
  "counterparty": {
    "name": "客户/供应商名称",
    "address": "地址",
    "phone": "电话"
  },
  "documentMeta": {
    "documentNumber": "单据编号",
    "date": "日期",
    "month": "所属月份（如有）",
    "otherMeta": {"key": "value"}
  },
  "table": {
    "headers": ["列1", "列2", ...],
    "rows": [["val1", "val2", ...], ...],
    "hasFormula": true/false,
    "formulaColumns": [{"colIndex": 7, "formula": "=G{row}*F{row}"}]
  },
  "summary": {
    "totalLabel": "合计/总计",
    "totalAmount": 123.45,
    "taxRate": "13%",
    "taxAmount": 16.05,
    "grandTotal": 139.50,
    "otherSummary": {"key": "value"}
  },
  "footer": {
    "notes": ["备注行1", "备注行2"],
    "signatures": ["制表人:XXX", "核准：", "送货方签名盖章：", "收货方签名盖章："]
  }
}

规则：
1. table.rows 中每行数据对应 headers 的列顺序，空值用 "" 表示
2. 如有公式列（如 金额=单价*数量），在 formulaColumns 中描述
3. 同一日期/订单号的多行数据，请如实逐行输出，不要合并
4. 忽略印章、签名图片、水印，但保留签名区的文本标签
5. 金额、数量等用数字，不要带货币符号
6. 只返回 JSON，不要返回其他内容
"""

TEMPLATE_FILL_PROMPT = """你是专业的财务文件数据提取与模板映射引擎。

用户提供了一个 Excel 模板和一份 PDF 原始单据。
你的任务：
1. 仔细阅读 PDF 图像，提取所有数据
2. 对照下方的 Excel 模板结构，将提取的数据映射到对应的单元格坐标
3. 只返回 JSON，不要返回任何其他内容

Excel 模板结构如下（每个单元格标注了角色）：
{template_structure}

## 单元格角色说明

- [LABEL]：固定标签文本（如 "核准："、"制表："）——**绝对不要覆盖**，把对应的值写入该标签旁边的空单元格。
  例如：F22="制表：" [LABEL]，G22 [EMPTY] → 把制表人姓名写入 G22
- [LABEL+VALUE]：合并单元格，内含多个"标签：___"槽位——**保留标签文字**，把 PDF 提取的值填入对应标签后面。
  例如：A2='电话：     传真：' → 填为 "电话：13544237178     传真：0752-7160718"
  例如：A5='地址：                    日期： ' → 填为 "地址：xxx路xxx号                    日期：2025-03-15"
- [PLACEHOLDER]：占位符文本（如 'xxx有限公司'）——**用 PDF 中提取的真实值整体替换**
- [EMPTY]：空单元格——可自由填入数据
- [FIXED]：固定文本——不要覆盖
- [DATA]：已有数据（如序号 1,2,3...）——通常不覆盖，除非 PDF 数据不同

## 返回 JSON 格式

{{
  "cells": [
    {{"ref": "A1", "value": "惠州市罗丰实业有限公司"}},
    {{"ref": "A2", "value": "电话：13544237178     传真：0752-7160718"}},
    {{"ref": "G22", "value": "林小姐"}}
  ],
  "tableRegion": {{
    "startRef": "B9",
    "rows": [
      ["0.8*57", null, 276, 1.0, null, null],
      ["1.0*85", null, 656, 2.0, null, "65M钢材料"]
    ]
  }},
  "summaryCells": [
    {{"ref": "F19", "value": 3486.00}}
  ]
}}

## 关键规则

1. **不要覆盖 [LABEL] 和 [FIXED] 单元格**。值应写入旁边的 [EMPTY] 单元格。
2. **[LABEL+VALUE] 单元格**必须保留原有标签文字，仅在标签后面填入提取的值。
3. **[PLACEHOLDER] 单元格**用真实值整体替换。
4. **tableRegion.startRef** 应指向表格数据区第一行、第一个数据列（跳过序号列）。
   例如模板的序号在 A 列且已预填 1-10，则 startRef 应为 "B9"（从规格列开始），rows 中不含序号。
5. **数据行数不受模板限制**：即使 PDF 数据行数超过模板预留的行数，也要把所有数据行都放入 rows，系统会自动扩行。
6. **列对齐非常重要——按位置对齐，不要按紧凑顺序排列**：
   rows 中每行的元素数量必须等于从 startRef 到模板最后一个数据列的列数，
   严格按照模板表头（[FIXED] 行）的列顺序一一对应。

   ⚠️ 对齐的正确方法：先看 PDF 图像中表格的视觉列位置，再匹配到模板的列。
   不要把数据向左"紧凑"填充——如果 PDF 中某一列视觉上为空，模板对应列必须填 null。

   示例：模板表头为 B='规格', C='公斤', D='数量/公斤', E='单价', F='金额', G='备注'，startRef="B9"。
   如果 PDF 中某行数据为：规格=0.8*57, 公斤列为空, 数量/公斤=276, 单价=1.0, 金额列为空, 备注列为空，
   则正确填法：["0.8*57", null, 276, 1.0, null, null]
   ❌ 错误填法：["0.8*57", 276, 1.0, null, null, null]  ← 这样276被错误地放入了"公斤"列

   请逐列比对 PDF 的列标题与模板表头的语义，确保每个值落入正确的模板列。
   如果 PDF 某列在模板中没有对应列，则忽略该 PDF 列。
7. **备注列**：PDF 表格中每行的备注/说明必须提取并填入对应行的备注列（通常是最后一列）。
   不要遗漏任何一行的备注。
8. **底部备注/说明**：从 PDF 中提取实际备注内容，填入模板的备注单元格。如果 PDF 的备注与模板默认文本不同，用 PDF 的内容替换。
9. 数值用数字类型，不要带货币符号。
10. 合并单元格写入左上角坐标。
11. 只返回 JSON，不要返回其他内容。
"""

TEMPLATE_PROMPTS = {
    "delivery_note": """你是一个专业的财务文件识别助手。请从上传的文件中提取送货单信息，并以 JSON 格式返回。
请提取以下字段：
- headers: 表头数组，例如 ["日期", "品名", "规格", "数量", "单价", "金额", "备注"]
- rows: 数据行数组，每行是一个对象
- summary: 摘要信息，包含 totalAmount(总金额), documentDate(单据日期), supplier(供应商/客户), documentType(单据类型), documentNumber(单据编号)

注意：请忽略文档中的盖章、印章、签名等信息，不需要提取这些内容。
只返回 JSON，不要返回其他内容。如果某个字段无法识别，请填写空字符串。""",
    "reconciliation": """你是一个专业的财务文件识别助手。请从上传的文件中提取对账单信息，并以 JSON 格式返回。
请提取以下字段：
- headers: 表头数组，例如 ["日期", "摘要", "借方金额", "贷方金额", "余额", "备注"]
- rows: 数据行数组，每行是一个对象
- summary: 摘要信息，包含 totalAmount(总金额), documentDate(单据日期), supplier(对方单位), documentType(单据类型), documentNumber(单据编号)

注意：请忽略文档中的盖章、印章、签名等信息，不需要提取这些内容。
只返回 JSON，不要返回其他内容。如果某个字段无法识别，请填写空字符串。""",
    "purchase_order": """你是一个专业的财务文件识别助手。请从上传的文件中提取采购单信息，并以 JSON 格式返回。
请提取以下字段：
- headers: 表头数组，例如 ["序号", "品名", "规格型号", "单位", "数量", "单价", "金额"]
- rows: 数据行数组，每行是一个对象
- summary: 摘要信息，包含 totalAmount(合计金额), documentDate(采购日期), supplier(供应商), documentType(单据类型), documentNumber(采购单号)

注意：请忽略文档中的盖章、印章、签名等信息，不需要提取这些内容。
只返回 JSON，不要返回其他内容。如果某个字段无法识别，请填写空字符串。""",
    "bank_statement": """你是一个专业的财务文件识别助手。请从上传的文件中提取银行流水/对账信息，并以 JSON 格式返回。
请提取以下字段：
- headers: 表头数组，例如 ["交易日期", "交易类型", "对方账户", "摘要", "收入", "支出", "余额"]
- rows: 数据行数组，每行是一个对象
- summary: 摘要信息，包含 totalAmount(期末余额), documentDate(账单日期), supplier(开户行), documentType(单据类型), documentNumber(账号)

注意：请忽略文档中的盖章、印章、签名等信息，不需要提取这些内容。
只返回 JSON，不要返回其他内容。如果某个字段无法识别，请填写空字符串。""",
    "payment_list": """你是一个专业的财务文件识别助手。请从上传的文件中提取支付清单信息，并以 JSON 格式返回。
请提取以下字段：
- headers: 表头数组，例如 ["序号", "收款方", "账号", "金额", "用途", "日期", "状态"]
- rows: 数据行数组，每行是一个对象
- summary: 摘要信息，包含 totalAmount(总支付金额), documentDate(支付日期), supplier(付款方), documentType(单据类型), documentNumber(批次号)

注意：请忽略文档中的盖章、印章、签名等信息，不需要提取这些内容。
只返回 JSON，不要返回其他内容。如果某个字段无法识别，请填写空字符串。""",
    "quotation": """你是一个专业的财务文件识别助手。请从上传的文件中提取报价单信息，并以 JSON 格式返回。
请提取以下字段：
- headers: 表头数组，例如 ["序号", "规格", "公斤", "数量/公斤", "单价", "金额", "备注"]
- rows: 数据行数组，每行是一个对象
- summary: 摘要信息，包含 totalAmount(合计金额), documentDate(报价日期), supplier(供应商/公司名称), documentType(单据类型), documentNumber(报价单号), contact(联系电话), address(地址)

注意：请忽略文档中的盖章、印章、签名、水印等信息，不需要提取这些内容。
只返回 JSON，不要返回其他内容。如果某个字段无法识别，请填写空字符串。""",
}
